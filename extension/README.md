# Extension

This folder contains the DealMate browser extension:

- manifest.json — browser extension manifest
- popup.html — popup UI
- popup.js — popup logic
- content.js — content script for detecting checkout pages and coupon fields
